services:
  - type: web
    name: velora-mcp-server
    env: docker
    region: oregon
    plan: free
    branch: main
    dockerContext: ./mcp-server
    dockerfilePath: ./mcp-server/Dockerfile
    envVars:
      - key: PORT
        value: 3000
